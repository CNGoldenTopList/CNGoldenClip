{
  "name": "cngoldenclip",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "description": "A userscript for CNGist admins to quickly log challenge completions from Bilibili search results, with automatic player matching and duplicate reminders.",
  "repository": "github:Diving-Fish/CNGoldenClip",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node tests/server.mjs"
  },
  "dependencies": { "pinyin-pro": "3.29.4" },
  "devDependencies": { "esbuild": "0.25.12" }
}
